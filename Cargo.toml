[package]
name = "swatches"
version = "0.1.0"
edition = "2021"
description = "Renderer-independent shared appearance specifications for desktop tools"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
toml = "0.8"

[dev-dependencies]
tempfile = "3"
